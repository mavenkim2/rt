#ifndef GPU_SCENE_H
#define GPU_SCENE_H

#include "graphics/vulkan.h"

namespace rt
{
enum class GeometryType;
struct Light;
struct MaterialNode;
struct Mesh;
struct ScenePrimitives;
struct PrimitiveIndices;

} // namespace rt

#endif
